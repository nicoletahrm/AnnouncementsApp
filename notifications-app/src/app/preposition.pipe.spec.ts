import { PrepositionPipe } from './preposition.pipe';

describe('PrepositionPipe', () => {
  it('create an instance', () => {
    const pipe = new PrepositionPipe();
    expect(pipe).toBeTruthy();
  });
});
